NEXT_PUBLIC_API_BASE_URL=/api

# Supabase Server-Only Configuration (Never exposed to browser)
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Secret / Private Service Role Key (Server-side only, bypasses RLS)
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_SCHEMA=personal_finance

